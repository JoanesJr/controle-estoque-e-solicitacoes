<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css">
    <title>Depósito</title>
</head>
<body>
    <div class="container">
        <div class="row justify-content-start mt-3">
            <div class="col-2">
                <a href="#" class="btn btn-primary"><strong><%= usuario %></strong></a>
                <a class="btn btn-danger" href="/sair"><strong class="m-4">Sair</strong></a>
                <input type="text" name="buscar" id="buscar" class="form-control mt-3">
            </div>
        </div>
        <div class="row justify-content-end mt-3">
            <div id="buttons-top" class="col-3 d-flex align-itemns-center">
                <a href="/administrador/add_usuario" class="btn btn-sm btn-outline-primary">Adicionar Usuário</a>
            </div>
        </div>
        </div>
        <div class="row justify-content-center mt-3">
            <div class="col-auto">
                <table class="table table-striped">
                    <tr class="text-center" id="header">
                        <th>Nome</th>
                        <th>Usuário</th>
                        <th>Senha</th>
                        <th>Cargo</th>
                        <th>Data Criação</th>
                        <th>Ações</th>
                    </tr>

                    <% for(let i = 0; i < usuarios.length; i++) { %>
                        <tr>
                            <td class="text-left"><%= usuarios[i].nome %></td>
                            <td class="text-center"><%= usuarios[i].usuario %></td>
                            <td class="text-center"><%= usuarios[i].senha %></td>
                            <td class="text-center"><%= usuarios[i].cargo %></td>
                            <td class="text-center"><%= usuarios[i].data_criacao %></td>
                            <td class="text-center">
                                <div class="row justify-content-center">
                                    <div class="col-3">
                                        <form action="/administrador/excluir" method="post">
                                            <div class="d-none">
                                                <input type="number" value="<%= usuarios[i].id %>" name="id" id="id">
                                            </div>
                                            <input type="submit" value="Excluir" class="btn btn-sm btn-danger">
                                        </form>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    <% } %>
                </table>

            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>